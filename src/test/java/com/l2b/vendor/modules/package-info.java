/**
 * Each feature is {@code data / domain / presentation}.
 * Environment (device, JDK, Allure, noReset) lives in that feature's {@code domain},
 * not in {@code BaseTest}.
 */
package com.l2b.vendor.modules;
